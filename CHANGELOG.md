# CNC Copilot FULL 3.1.0 — WIP-05

## Guided result cards at the machine
- Final result is no longer rendered as one long stack of all operation cards.
- Only the current operation card is shown; future operations stay locked until previous operations are confirmed on the machine.
- Added operation progress rail with `проверено / сейчас / после предыдущей` states.
- `Всё отлично` confirms the current pass. When every pass of the operation is confirmed, Copilot opens the next operation automatically.
- Vibration, squeal, heat, poor surface, chip control, wear and spindle-load feedback recalculate only the current pass in place and require a new trial pass.
- Previous verified operations remain available for review.
- Result footer shows when the entire route has been confirmed.
- Existing calculation formulas and WIP-04 machine envelope are preserved.


## WIP-04 · подтверждённый профиль CK52PT-Y
- Удалён старый placeholder 11 кВт из профиля станка.
- Добавлены данные с шильдиков: Siemens SIMOTICS M 1PH8137-1DD02-0CA1, S1 17/19.5/22/24 кВт, nmax 8000 об/мин, Mmax 405 Н·м, SINAMICS S120 Combi, BK-1552 6000 об/мин / 45 kgf/cm².
- Для автоматического расчёта нагрузки используется консервативная база 17 кВт; 24 кВт не трактуются как постоянная мощность шпинделя без подтверждённого передаточного отношения.
- G96/LIMS теперь вычисляется как минимум из лимита станка, механических компонентов и текущего патрона/кулачков.
- Экран проверки показывает, какой именно компонент ограничивает обороты.

# CNC Copilot FULL 3.1.0 — WIP-03

- Guided Workflow расширен с 5 до 8 отдельных экранов: станок → заготовка → операции → инструмент → размеры операций → стратегия → расчёт → результат.
- Выбор операций и выбор инструмента больше не смешаны с настройкой геометрии.
- Каждая операция на шаге размеров показывается отдельно; переход к следующей блокируется, пока текущая геометрия не валидна.
- Добавлена очередь операций с индикаторами готовности и возможностью вернуться к нужной операции.
- Добавлено явное условие «Задняя бабка»: Авто / Подпереть / Без бабки; режим сохраняется в проекте и влияет на рекомендацию.
- Твёрдость HB сделана действительно необязательной: для перехода достаточно материала, фактического Ø и длины; при пустом HB расчёт использует справочное значение материала.
- Расчёт вынесен в отдельный экран предварительной проверки.
- Результат остаётся на отдельном шаге с пооперационной диагностикой первого прохода.

# 3.1.0 WIP · Guided Workflow

- Новый проект: пользовательские поля стартуют пустыми, без демо Ø50 / L100 / HB.
- Ручное добавление инструмента: реальные значения не подставляются, примеры только placeholder.
- Порядок операций: «Сырьё / было» → «По чертежу / должно стать»; целевые размеры больше не генерируются фиктивно.
- Геометрия операций валидируется до перехода к стратегии; наружное точение и расточка проверяют направление изменения диаметра.
- Для последовательных наружных операций исходный Ø автоматически наследуется из предыдущего целевого Ø, пока пользователь не переопределил его вручную.

# История изменений

## 3.0.6

- Исправлено кэширование иконки при установке PWA на экран «Домой» в iOS.
- Apple Touch Icon переведён на уникальные версионированные имена файлов для размеров 120/152/167/180 px.
- Manifest и PWA-иконки также получили новые URL, чтобы Safari не мог использовать старый кэш.
- Для PWA-метаданных и иконок сервер выставляет `no-store, no-cache, must-revalidate`.
- Логика расчётов, ИИ, синхронизации, удаления и авторизации не изменена.

# Журнал изменений

## 3.0.6

- Установлена новая фирменная иконка CNC Copilot / YEROSHOV CNC, выбранная пользователем.
- Обновлены PWA-иконки 192×192, 512×512 и 1024×1024.
- Обновлены Apple Touch Icon, favicon PNG/ICO и SVG-fallback.
- PWA-кэш обновлён до v3.0.6, чтобы устройства получили новую иконку после развёртывания.
- Функциональная логика 3.0.4 не изменялась.

# История изменений

## 3.0.6

- Исправлен `package-lock.json`: восстановлены реальные версии транзитивных npm-зависимостей из рабочей 3.0.2.
- Смена версии CNC Copilot больше не изменяет версии сторонних пакетов.
- Сохранено исправление синхронизации удалений из 3.0.3.

## 3.0.3

- Удаление инструмента и проекта синхронизируется как отдельная метка удаления с временем.
- Облачная копия больше не может вернуть удалённую локально карточку после merge.

## 3.0.2

- Новый расчёт и ручное добавление инструмента больше не выбирают «Торцовку» и «Наружку» автоматически.
- Если пользователь ничего не выбрал, операции не подставляются скрытым запасным значением.
- ИИ по-прежнему может предложить операции как результат распознавания.

## 3.0.1

- Полная русификация пользовательских статусов, сканера, единиц измерения и сообщений ошибок.
- Унифицированы индикаторы «Локально / Онлайн», «Готово» и статусы карточек.
- PWA-кэш обновлён, чтобы после развёртывания не оставались подписи версии 3.0.0.
- Расчётное ядро, Passkey, синхронизация, ИИ/ИИ-модуль и логика «Умного шкафа» не изменялись.

## 3.0.0

- Объединена серверная архитектура и авторизация 2.3 с CNC-процессом FULL 1.1.2.
- Добавлены явные режимы «Локально» и «Онлайн по запросу».
- Добавлено хранилище `/api/sync` через Railway/PostgreSQL для станка, «Умного шкафа» и проектов.
- Добавлен совместимый маршрут `/api/scan-insert` для сканера FULL.
- ИИ-распознавание принимает до четырёх фотографий за одно сканирование, подтверждение оператором остаётся обязательным.
- Добавлен анимированный экран входа Passkey/Face ID в стиле 2.3.
- Добавлена локальная разблокировка доверенного устройства через Passkey для офлайн-PWA.
- Сохранены адаптивный нижний Dock, маршрут из нескольких операций, мультивыбор инструмента, цветовые карточки материалов, требования, проекты и локальные резервные копии.
- PWA-кэш обновлён до v3.0.0, а запросы API оставлены вне кэша service worker.

## 2.3.0

- Результат расчёта, история, шаринг и PDF приведены к станочному формату: только `F` в `mm/rev` с тремя знаками и `S` в целых `rpm`.
- Предупреждения технологической безопасности сохранены без изменений.
- ИИ материалов теперь распознаёт только материал заготовки; коробки пластин и маркировки державок безопасно перенаправляются в «Инструмент».
- ISO-группы P/M/K/N/S/H получили узнаваемые цвета коробок пластин в каталоге, ИИ-результате и текущем выборе.
- Каталог инструмента разделён фильтрами «Пластины» и «Державки»; добавлены тип, исполнение, хвостовик и совместимые коды пластин.
- Фиксированные верхняя и нижняя панели версии 2.2.4 сохранены; нижняя панель никогда не скрывается.

## 2.2.4

- Верхняя и нижняя панели вынесены в независимые фиксированные слои с повышенной специфичностью, поэтому общий стеклянный стиль больше не может сбросить их позиционирование.
- Нижняя панель постоянно полностью видна над Home Indicator на любой позиции страницы.
- Шапка получила непросвечивающую стеклянную подложку: карточки больше не читаются сквозь неё.
- Бонус: повторный тап по активному разделу или тап по свободной области шапки плавно возвращает страницу наверх без лишнего перерендера.

## 2.2.3

- Нижняя панель закреплена постоянно в полностью раскрытом виде; автоскрытие и компактный режим удалены.
- Буква C в шапке заменена полноценной фирменной иконкой приложения в отдельной стеклянной капсуле.
- Добавлены регрессионные проверки, запрещающие повторное появление логики скрытия нижней панели.

## 2.2.2

- Исправлена геометрия шапки на iPhone 14 Pro: safe-area применяется один раз, шапка больше не сдвигается и не перекрывает заголовок страницы.
- Основная PWA-ориентация изменена на портретную.
- Нижняя панель при прокрутке не исчезает, а сжимается в компактную стеклянную капсулу; тап разворачивает её обратно.
- Убраны фильтры размытия с движущейся нижней панели для стабильной резкости в iOS WebKit.

## 2.2.1

- Убрано размытие соседних карточек при выборе операции: текст и иконки остаются резкими на iPhone.
- Исправлено повторное открытие одной и той же операции после закрытия формы расчёта.
- Анимация выбора сохранена без CSS-фильтров, вызывающих размытие в iOS WebKit.

## 2.2.0

- Нижняя панель закреплена у края iPhone, прячется при прокрутке вниз и возвращается при прокрутке вверх или остановке.
- Активный раздел получил интерактивную стеклянную подсветку и плавный индикатор.
- Верхняя панель закреплена отдельно под системной зоной iPhone и остаётся резкой при прокрутке.
- Неочевидная кнопка заменена понятной иконкой выхода через дверь.

## 2.1.0

- Исправлено сильное размытие текста и кнопок липкой верхней панели на iPhone/iOS WebKit.
- Стеклянный фон шапки вынесен в отдельный слой, а содержимое рендерится поверх него без фильтра.
- Добавлена фирменная иконка установки PWA на рабочий стол iPhone.
- Добавлена стеклянная карточка автора: Ерошов Иван и контактный телефон.
- Добавлены тактильная анимация выбора операций и плавные Liquid Glass-переходы окон.

## 2.0.0

- Новый macOS Liquid Glass-интерфейс и спокойная CNC-анимация на экране авторизации.
- ИИ-распознавание текста материалов, бирок, сертификатов, упаковок пластин и маркировки инструмента.
- Явное подтверждение всех ИИ-распознанных данных оператором.
- Расширенный каталог материалов и безопасные резервный ISO-профиль-позиции.
- Оценка времени резания, мощности шпинделя и загрузки станка.
- Станочные строки G95/S/F/G96/LIMS.
- Настройки станка CK52PT-Y / SINUMERIK 828D и мощности шпинделя.
- Настоящие PDF-отчёты, совместимые с iPhone Share/Files.
- Управление Passkeys, сессиями, паролем и кодами восстановления.
- Улучшенная блокировка, светлая/тёмная тема, PWA и адаптивность iPhone.

## 3.1.0 WIP-06 — Guided Workflow visual polish
- Added directional stage transitions and an animated 8-step progress line without touching calculation logic.
- Active step auto-centers in the mobile stepper and exposes `aria-current` for accessibility.
- Material selection now progressively reveals the actual stock fields; no stock form is shown as if it were already active before material selection.
- Operation catalog enters as a lightweight cascade; single-operation setup gets a focused-card transition.
- Mobile step controls use a blurred sticky action tray so the next action stays thumb-reachable.
- Added `prefers-reduced-motion` handling; all data and controls remain available without animation.


## WIP-07 · End-to-end scenario audit
- Шаг 5 теперь блокирует будущие операции, пока предыдущая геометрия не заполнена.
- Ø/припуск превращаются в физический план съёма: Copilot считает количество черновых проходов и реальный ap на проход.
- При корректировке режима у станка число проходов пересчитывается вместе с S/f/ap.
- В операции «Черновая + чистовая» чистовой проход нельзя подтвердить раньше чернового.
- Перед расчётом появился краткий маршрут: геометрия + фактически рекомендуемый инструмент по каждой операции.
- Если задняя бабка планируется без центровки, preflight явно предупреждает оператора.

## WIP-08 · Pass-specific tool assignment
- Для операций «Черновая + чистовая» инструмент теперь назначается отдельно на черновой и чистовой проход.
- Черновой инструмент больше никогда не подменяет чистовой только из-за общего `toolId`.
- Автоподбор фильтрует инструмент по ISO, операции и назначению прохода; если в выбранном наборе нет чистового инструмента, разрешён fallback только к совместимому инструменту из шкафа/каталога.
- Шаг «Размеры операций» блокирует переход дальше, если для любого требуемого прохода нет совместимой сборки.
- В ручном добавлении и после ИИ-сканирования оператор явно подтверждает назначение пластины: черновая / чистовая / оба режима.
- Старые локальные инструменты из WIP-07 и более ранних версий не получают доверие к «черновая + чистовая» автоматически: в шкафу появился быстрый профиль «Черн. / Чист. / Оба».
- Сохранённые маршруты со старым единым `toolId` мигрируют безопасно: инструмент переносится только в тот проход, для которого он действительно совместим.
